export enum GaHitVariant {
  PAGE_VIEW = 'pageview',
  EVENT = 'event',
}

export enum EventCategory {
  TRANSITION_PAGE = 'transition-page',
}

export enum EventAction {
  LOADED = 'loaded',
  PROCEEDED = 'proceeded',
}
